self.transitionInDuration = 200;
self.transitionOutDuration = 600;// Way back of focus is relative to focus position, so we need to make it longer

self.hDistanceMax = 120;
self.vDistanceMax = 80;

self.transitionDuration = 0;
self.transitionStep = 0;
self.focus = obj_minch;
